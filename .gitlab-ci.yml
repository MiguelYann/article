image: docker:stable
services:
    - docker:18.09-dind
stages: 
    - verify 
    - build_app
    - build_container
    - package
    - release
 


⚙ building: 
    image: node
    only: 
        - master
    stage: build_app 
    script: 
        - npm install
        - npm run build --prod
    artifacts:
        paths:
            - dist/

🚦 test: 
    dependencies: 
        - ⚙ building
    stage: verify
    only: 
        - master
    script: 
        - npm run test
🚢 dockerise:
    dependencies:
        - ⚙ building

    only: 
        - master
    stage: build_container
    script: 
    - docker info
    - docker build -t angularapp:1.0.0 .
    - mkdir imageDocker/
    artifacts:
        paths:
            - imageDocker



